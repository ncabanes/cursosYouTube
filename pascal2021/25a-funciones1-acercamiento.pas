(* Acercamiento a las "funciones" y los "procedimientos": necesidad *)

program Procedimientos1pre;

begin
    writeLn( 'Hola, bienvenido/a al curso' );
    writeLn( '==-----------------------==' );
    WriteLn;

    writeLn( 'Espero que te resulte util');
    writeLn( '==-----------------------==' );
    WriteLn;

    writeLn( 'No tengas miedo de preguntar' );
    writeLn( '---------------------------' );
    WriteLn;
end.

